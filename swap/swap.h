#include <iostream.h>

// because of the ristriction of problem,   
//		we can use only "one" variables.  
#define PRELUDE \
	int tmp; \
	if (flag < 0) { \
		cerr << "proc:: set -x flag for divide point" << endl;\
	exit(1); \
	}

